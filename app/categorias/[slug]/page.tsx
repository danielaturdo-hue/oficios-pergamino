import {notFound} from 'next/navigation';import {categories,professionals} from '@/lib/data';import {ProfessionalCard} from '@/components/ProfessionalCard';import Link from 'next/link';
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=categories.find(x=>x.slug===slug);if(!c)return notFound();const ps=professionals.filter(p=>p.category===c.name);return <main className="py-10"><div className="container"><div className="max-w-3xl"><span className="text-5xl">{c.icon}</span><h1 className="text-4xl font-black mt-4">{c.name} en Pergamino</h1><p className="muted text-lg mt-2">Encontrá profesionales y servicios de {c.name.toLowerCase()} en Pergamino.</p></div><div className="flex gap-3 mt-6"><Link href="/solicitar" className="btn btn-primary">Publicar un trabajo</Link><Link href="/buscar" className="btn btn-secondary">Buscar en todo Pergamino</Link></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4 mt-8">{ps.length?ps.map(p=><ProfessionalCard key={p.id} p={p}/>):<div className="card p-8 md:col-span-2 lg:col-span-3">Todavía no hay profesionales de ejemplo en esta categoría. ¡Podés ser el primero!</div>}</div></div></main>}
